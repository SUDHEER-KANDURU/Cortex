// Dashboard route loading — shown while the dashboard bundle and initial data load
import { PageLoader } from '@/components/shared/BrandedLoader';

export default function DashboardLoading() {
  return <PageLoader />;
}
